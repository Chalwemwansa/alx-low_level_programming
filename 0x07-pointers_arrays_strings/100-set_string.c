#include "main.h"
#include <stdio.h>
/**
 * set_string - pointer to another pointer
 * Return: no return
 * @s: variable used in the code
 * @to: variable used in the code
 */
void set_string(char **s, char *to)
{
**s = *to;
}
