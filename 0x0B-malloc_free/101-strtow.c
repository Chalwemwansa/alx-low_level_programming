#include "main.h"
#include <stdlib.h>
/**
 * strtow - splits string into words
 * Return: 0
 * @str: variable used in the code
 */
char **strtow(char *str)
{
if (str == NULL || str == "")
	return (NULL);
return (0);
}
