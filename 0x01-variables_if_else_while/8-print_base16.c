#include <stdio.h>
/**
*main - program prints all the numbers of base 16 followed by a new line
*Return: 0
*/
int main(void)
{
int i, p;
for (i = 48; i <= 57; i++)
putchar(i);
for (p = 97; p <= 102; p++)
putchar(p);
return (0);
}
